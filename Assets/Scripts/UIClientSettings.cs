using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Handles displaying settings as UI
public class UIClientSettings : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }
    // Update is called once per frame
    void Update()
    {
        
    }
    //Fills the settings UI GB
    void FillPane()
    {
        
    }
    //Make sure the pane matches the given settings. This may never need use
    void UpdatePane()
    {

    }
    //Generate UI Object to match setting
    void GenerateUIObject(dynamic item)
    {
        GameObject uiObject = new GameObject();
        //Follow to 
        switch (0)
        {
            default:
                break;
        }
    }
}
